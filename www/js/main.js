$(function () {
/*    $(document).on('DOMNodeInserted', function (e) {
        if ($(e.target).is('input') && $(e.target).hasClass('datepicker')) {
            $(e.target).datepicker({format: 'dd.mm.yyyy'});
        }
    });*/
});
